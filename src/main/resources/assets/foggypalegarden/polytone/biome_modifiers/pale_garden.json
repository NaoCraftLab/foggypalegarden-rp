{
  "targets": ${
  specPaleGardenTargets
},
  "fog_radius": "lerp(step(SKY_LIGHT, ${minSkyLight}) * step(${maxHeight} - POS_Y, 0), 1, ${fogRadius})",
  "fog_fade": "lerp(step(SKY_LIGHT, ${minSkyLight}) * step(${maxHeight} - POS_Y, 0), 1, ${fogFade})"
}
